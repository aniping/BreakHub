# BreakHub

BreakHub 在不改变业务调用默认可用性的前提下，按规则捕获接口调用，并允许在执行前后暂停、检查和调整本次调用。

## Language

**条件来源（Condition Source）**:
断点条件所检查的调用内容域。`params` 表示原始入参，`result` 表示业务正常返回的原始出参；after 断点可组合两种来源，before 断点只能检查 `params`。业务异常不属于 `result`。
_Avoid_: 条件目标、数据源

**条件字段路径（Condition Field Path）**:
条件来源对象内以点分隔的字段位置。它只穿过对象字段，不表示来源根值，也不包含数组索引；入参与出参遵守相同的路径边界。
_Avoid_: JSONPath、根路径

**未验证条件（Unverified Condition）**:
字段路径尚未被历史调用样本证实存在的断点条件。它仍是可创建、可执行的有效规则；Web 与 Agent Skill 都可配置这类条件，并以非阻塞告警说明缺少证据。
_Avoid_: 非法条件、无效条件

**丢弃条件（Discarded Condition）**:
写入 before 断点时因引用 `result` 而从规则中移除的条件。写入仍然成功，但必须告知用户被移除的内容；若所有条件都被丢弃，该断点成为无条件断点。
_Avoid_: 不适用条件、休眠条件

**参考调用（Reference Interaction）**:
用户明确选择、用于查看条件字段和值证据的一次历史调用。入参证据和出参证据必须来自同一次调用；参考调用只辅助配置，不限制未验证条件的创建。
_Avoid_: 合并样本、验证门槛

**命中快照（Hit Snapshot）**:
断点命中时冻结的规则定义与精简求值证据。每条条件保留来源、字段路径、操作符、期望值以及实际相等值或实际命中的候选值，之后修改或删除规则不会改变快照。
_Avoid_: 当前规则、完整 Payload 副本
