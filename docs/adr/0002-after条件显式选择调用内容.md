# After 条件显式选择调用内容

每条断点条件显式以 `source` 选择原始入参 `params` 或业务正常返回的原始出参 `result`，after 断点可把两种来源按 AND 组合，before 断点只使用入参。来源属于条件而不是由暂停点隐式决定，这既保留了 after 按输入范围筛选的能力，也允许同名输入输出字段共存且含义明确；写入 before 断点时的出参条件按 ADR-0001 丢弃。
